Deep Learninig Homework 4

Teil 1
vgg = torchvision.models_vgg19()
f = vgg.features[:31]
phi = f(x)

Teil 2
y_hat = P(x,y|image) -> likelihood
max Log-Likelihood!
y = features
max Log-Likelihood = sum(t(y_hat)*y)
